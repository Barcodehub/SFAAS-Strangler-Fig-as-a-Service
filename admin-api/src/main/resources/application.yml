server:
  port: 8081

spring:
  application:
    name: strangler-admin-api
  
  datasource:
    url: jdbc:postgresql://localhost:5432/sfaas
    username: sfaas
    password: sfaas123
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
